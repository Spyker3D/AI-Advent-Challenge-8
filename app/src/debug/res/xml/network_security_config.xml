<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Educational debug exception for a configurable HTTP VPS and LAN Ollama.
         TODO: require HTTPS for the VPS before shipping a production build. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
