const http = require("http"); const PORT = 3000; const tasks = { "AI-17": { status: "In Progress", title: "Implement first custom MCP tool" }, "AI-18": { status: "Todo", title: "Deploy MCP server to VPS" }, "BUG-1": { status: "Done", title: "Fix Android cleartext network config" } }; function createJsonRpcResponse(id, result) { return { jsonrpc: "2.0", id, result }; } function createJsonRpcError(id, code, message) { return { jsonrpc: "2.0", id, error: { code, message } }; } function handleToolsList(id) { return createJsonRpcResponse(id, { tools: [ { name: "get_task_status", description: "Returns status and short details for a task by taskId", inputSchema: { type: "object", properties: { taskId: { type: "string", description: "Task identifier, for example AI-17" } }, required: ["taskId"] } } ] }); } function handleToolsCall(id, params) { const toolName = params && params.name; const args = params && params.arguments ? params.arguments : {}; if (toolName !== "get_task_status") { return createJsonRpcError(id, -32601, `Unknown tool: ${toolName}`); } const taskId = args.taskId; if (!taskId) { return createJsonRpcError(id, -32602, "Missing required parameter: taskId"); } const task = tasks[taskId]; const text = task ? `Task ${taskId}: ${task.status}. ${task.title}.` : `Task ${taskId} was not found.`; return createJsonRpcResponse(id, { content: [ { type: "text", text } ] }); } function handleMcpRequest(body) { let request; try { request = JSON.parse(body); } catch (error) { return createJsonRpcError(null, -32700, "Parse error"); } const { id, method, params } = request; if (method === "tools/list") { return handleToolsList(id); } if (method === "tools/call") { return handleToolsCall(id, params); } return createJsonRpcError(id, -32601, `Method not found: ${method}`); } const server = http.createServer((req, res) => { if (req.method !== "POST" || req.url !== "/mcp") { res.writeHead(404, { "Content-Type": "application/json" }); res.end(JSON.stringify({ error: "Use POST /mcp" })); return; } let body = ""; req.on("data", chunk => { body += chunk; }); req.on("end", () => { console.log("Incoming MCP request:"); console.log(body); const response = handleMcpRequest(body); console.log("MCP response:"); console.log(JSON.stringify(response, null, 2)); res.writeHead(200, { "Content-Type": "application/json", "Access-Control-Allow-Origin": "*" }); res.end(JSON.stringify(response)); }); }); server.listen(PORT, () => { console.log(`Local MCP server is running on http://localhost:${PORT}/mcp`); });